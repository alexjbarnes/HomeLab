services:
  git-clone:
    image: alpine/git
    volumes:
      - type: bind
        source: /config-repo
        target: /repo
        read_only: false
    command: rm /repo/* -r && clone https://github.com/alexjbarnes/HomeLab.git /repo
    restart: "no"  # Run once and stop
