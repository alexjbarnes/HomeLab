services:
  vault-sync:
    image: ghcr.io/alexjbarnes/vault-sync:latest
    env_file:
      - .env
    volumes:
      - vault-sync:/home/nonroot/.vault-sync
    restart: unless-stopped

volumes:
  vault-sync:
